import { Controller } from '@nestjs/common';

@Controller('admin')
export class AdminController {
    // sync artist data with elastic
    // get all data of artist
    // sync director data with elastic
    // get all data of director
    // remove Directly From Elastic
}
